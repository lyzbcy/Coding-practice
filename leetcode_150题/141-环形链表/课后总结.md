# 课后总结

## 📝 我发布的题解
https://leetcode.cn/problems/linked-list-cycle/solutions/3840244/gui-tu-sai-pao-kuai-man-zhi-zhen-jie-hua-b404/
---

## 复盘感想
思路是快慢指针龟兔赛跑
主要是链表有点忘了 正好复习一下

## 💡 思路关键点

### 核心算法
- **快慢指针（Floyd判圈算法）**
- 使用两个指针 `slow` 和 `fast`，分别以不同速度遍历链表
- 如果链表有环，快指针最终会追上慢指针（相遇）

### 关键代码逻辑
```c
slow = head;
fast = head->next;

while (fast != NULL && fast->next != NULL) {
    if (slow == fast) {
        return true;  // 相遇，有环
    }
    slow = slow->next;      // 慢指针移动一步
    fast = fast->next->next; // 快指针移动两步
}
return false;  // 快指针到达末尾，无环
```

### 时间复杂度
- **O(n)**：n 是链表的长度，最坏情况下需要遍历整个链表

### 空间复杂度
- **O(1)**：只使用了两个指针变量

---

## 🔄 复盘感想


### 易错点提醒
- ⚠️ 注意：快指针每次移动两步，需要检查 `fast->next` 是否为 NULL
- ⚠️ 边界条件：空链表和单节点链表都不存在环
- ⚠️ 初始条件：避免 `slow` 和 `fast` 初始时相等导致误判

---

## 📚 相关题目

- [ ] 142. 环形链表 II（找环的入口）
- [ ] 202. 快乐数（快慢指针应用）
- [ ] 876. 链表的中间结点（快慢指针应用）

---

