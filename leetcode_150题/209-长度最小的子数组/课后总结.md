# 课后总结

## 📝 我发布的题解

- [ ] 已发布题解链接：_________________________
- [ ] 提交记录截图：_________________________

---

## 💡 思路关键点

### 核心算法
- **滑动窗口（双指针）**
- 使用两个指针 `left` 和 `right` 维护一个窗口 `[left, right]`
- `right` 不断向右扩展，增加窗口内的元素和
- 当窗口内的元素和 `>= target` 时，尝试缩小窗口（`left` 右移），寻找更小的长度
- 在窗口缩小的过程中，记录满足条件的最小长度

### 关键代码逻辑
```c
int minSubArrayLen(int target, int* nums, int numsSize) {
    int left = 0;
    int sum = 0;
    int minLen = INT_MAX;
    
    for (int right = 0; right < numsSize; right++) {
        sum += nums[right];  // 扩展窗口
        
        while (sum >= target) {  // 收缩窗口
            minLen = min(minLen, right - left + 1);
            sum -= nums[left];
            left++;
        }
    }
    
    return minLen == INT_MAX ? 0 : minLen;
}
```

### 时间复杂度
- **O(n)**：每个元素最多被 `left` 和 `right` 各访问一次
- `right` 从 `0` 到 `n-1`，共 `n` 次
- `left` 最多从 `0` 到 `n-1`，共 `n` 次
- 总时间复杂度：O(n)

### 空间复杂度
- **O(1)**：只使用了常数额外空间（`left`、`right`、`sum`、`minLen`）

### 关键技巧
1. **滑动窗口技巧**：
   - 使用两个指针维护一个窗口
   - 根据窗口状态决定扩展或收缩

2. **子数组 vs 子序列**：
   - **子数组**：必须是连续的（本题要求）
   - **子序列**：可以不连续
   - 滑动窗口保证连续性

3. **为什么可以用滑动窗口？**：
   - 所有元素都是正整数，保证了窗口扩展时和单调递增
   - 当窗口和 `>= target` 时，继续扩展只会让和更大，不会更优
   - 因此可以安全地缩小窗口，寻找更小的长度

4. **为什么用 `while` 而不是 `if`？**：
   - 缩小窗口后可能仍然满足 `sum >= target`
   - 需要继续缩小，直到 `sum < target`

---

## 🔄 复盘感想

### 初次提交的问题
（待填写）

### 改进后的收获
（待填写）

### 易错点提醒
- ⚠️ **子数组 vs 子序列**：题目要求的是子数组（必须连续），不能排序
- ⚠️ **滑动窗口条件**：使用 `while` 而不是 `if` 来缩小窗口
- ⚠️ **窗口长度计算**：使用 `right - left + 1`，不是 `right - left`
- ⚠️ **边界处理**：如果未找到，返回 `0`（`minLen == INT_MAX ? 0 : minLen`）
- ⚠️ **为什么可以用滑动窗口**：所有元素都是正整数，保证了和的单调性

---

## 📚 相关题目

- [ ] 3. 无重复字符的最长子串（滑动窗口）
- [ ] 76. 最小覆盖子串（滑动窗口进阶）
- [ ] 30. 串联所有单词的子串（滑动窗口变种）
- [ ] 904. 水果成篮（滑动窗口）

---

## 🎯 下一步计划

- [ ] 完成进阶问题：O(n log n) 解法（前缀和+二分查找）
- [ ] 练习相关题目，巩固滑动窗口技巧
- [ ] 理解滑动窗口的适用条件（为什么需要正整数）

---

**学习日期：** _______________  
**完成状态：** ⬜ 未完成  ✅ 已完成



