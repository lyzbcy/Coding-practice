# 动画演示使用说明

## 🎬 如何运行动画

1. **安装依赖**

```bash
pip install matplotlib numpy
```

2. **运行程序**

```bash
python 动画演示.py
```

3. **选择示例**

程序提供四个内置选项：

- 选项 1：示例 1（基本滑动窗口）  
  `target = 7`, `nums = [2,3,1,2,4,3]` → `2`

- 选项 2：示例 2（单个元素满足）  
  `target = 4`, `nums = [1,4,4]` → `1`

- 选项 3：示例 3（无解情况）  
  `target = 11`, `nums = [1,1,1,1,1,1,1,1]` → `0`

- 选项 4：自定义输入（支持任意 target 和数组）

---

## 🎮 手动控制方式

| 按键 | 功能 |
|------|------|
| 空格 / → / Enter | 下一步 |
| ← | 上一步 |
| Home | 跳转到第一步 |
| End | 跳转到最后一步 |
| Q / Esc | 退出程序 |

> ⚠️ 使用键盘前，请先点击动画窗口以确保焦点在窗口上。

---

## 📊 动画元素说明

- **绿色区域**：当前窗口内的元素（`[left, right]` 范围内的元素）
- **灰色区域**：窗口外的元素
- **蓝色箭头**：`left` 指针（窗口左边界）
- **红色箭头**：`right` 指针（窗口右边界）
- **信息面板**：显示窗口范围、窗口元素、窗口和、最小长度、目标值

---

## 🔁 动画流程

1. **初始化**：
   - 显示原始数组和目标值
   - 初始化 `left=0`, `sum=0`, `minLen=∞`

2. **扩展窗口**：
   - `right` 指针向右移动
   - 将 `nums[right]` 加入窗口
   - 更新窗口和 `sum`

3. **收缩窗口**：
   - 当 `sum >= target` 时：
     - 更新最小长度 `minLen`
     - 尝试缩小窗口（`left` 右移）
     - 移除 `nums[left]`，更新窗口和
   - 重复直到 `sum < target`

4. **结果展示**：
   - 显示最终的最小长度
   - 如果未找到，显示 `0`

---

## 💡 学习建议

1. **第一次观看**：选择示例 1（`[2,3,1,2,4,3]`），观察：
   - 窗口如何扩展和收缩
   - 最小长度如何更新
   - 为什么可以安全地缩小窗口

2. **第二次**：尝试示例 2（`[1,4,4]`），重点观察：
   - 单个元素就满足条件的情况
   - 窗口如何快速缩小到最小长度

3. **第三次**：输入自定义测试，例如：
   - `target=5, nums=[1,2,3,4,5]`（窗口逐步缩小）
   - `target=10, nums=[1,2,3,4,5]`（无解情况）
   - `target=3, nums=[1,1,1,1,1]`（需要多个元素）

4. **最后**：结合 `教学文档.md` 理解：
   - 滑动窗口的核心思想
   - 为什么所有元素都是正整数很重要
   - 时间复杂度为什么是 O(n)

5. **深入理解**：观察每一步中：
   - 窗口和的变化
   - 最小长度的更新时机
   - 窗口扩展和收缩的条件

---

## ❓ 常见问题

- **键盘无响应？**  
  请确保点击动画窗口使其获得焦点。如果仍无效，尝试重新运行或切换 Python 终端。

- **中文显示异常？**  
  修改脚本中的 `plt.rcParams['font.sans-serif']`，换成系统可用字体。

- **无法显示窗口？**  
  在无图形界面的环境中，可以使用 `plt.savefig` 导出每一步，但更推荐在本地运行。

- **数组太长显示不全？**  
  动画会自动调整显示范围，但如果数组过长，建议使用较短的测试用例。

- **为什么使用滑动窗口？**  
  因为所有元素都是正整数，窗口扩展时和单调递增，可以安全地缩小窗口寻找更小的长度。

- **为什么用 `while` 而不是 `if` 来缩小窗口？**  
  因为缩小窗口后可能仍然满足 `sum >= target`，需要继续缩小直到 `sum < target`。

- **时间复杂度为什么是 O(n)？**  
  每个元素最多被 `left` 和 `right` 各访问一次，所以是 O(n)。

- **如果数组中有负数，滑动窗口还适用吗？**  
  不适用。因为窗口扩展时和不一定单调递增，无法安全地缩小窗口。需要使用其他方法，如前缀和+二分查找。

---

**祝学习顺利，玩得开心！** 🎉



