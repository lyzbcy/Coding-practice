我发布的题解  
（在此填写题解链接）

## 最终思路

- 记录 `total = sum(gas) - sum(cost)` 判断是否存在解
- 线性扫描维护 `tank`，一旦 `tank < 0` 就把 `start` 推进到下一站并清空油箱
- 返回最终的 `start`（若 `total < 0` 则返回 `-1`）

## 思路关键点

1. `total`、`tank`、`start` 三个状态的含义与更新顺序
2. 为什么 `tank` 为负就可以直接跳过整段区间
3. 边界情况：单一站点、全部 0、无解

> ✅ 完成题目后，可在这里补充更详尽的推导或手写模拟截图链接。

## 复盘感想

- 今天在……（记录踩坑、灵光一现或与动画联动的收获）
- 下一次做环路/贪心题时，优先思考“总量够不够 + 局部是否亏油”的双判定


