我发布的题解  
（待填写题解链接）

思路关键点
- 先遍历 `magazine`，使用长度为 26 的数组统计每个字母的库存
- 再遍历 `ransomNote`，逐个扣减库存，若出现负数立即返回 `false`
- 记得在声明数组后用 `{0}` 或 `memset` 清零，避免垃圾值干扰
- 可在进入计数流程前先比较长度，若 `ransomNote` 更长可直接返回 `false`

## 复盘感想
（记录本次刷题的困惑、突破和需要回顾的知识点）


