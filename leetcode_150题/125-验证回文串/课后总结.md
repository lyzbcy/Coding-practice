我发布的题解
https://leetcode.cn/problems/valid-palindrome/solutions/3840169/shuang-zhi-zhen-0msjie-hui-wen-chuan-by-b1lmv/

## 复盘感想
这题比较简单 我思路完全正确
就是计算字符串长度忘了

严重错误：sizeof(s) 使用错误 ⚠️
int size=sizeof(s);  // ❌ 错误！
问题分析：

sizeof(s) 返回的是指针变量的大小（通常是 4 或 8 字节），而不是字符串的长度
例如：sizeof(s) 可能返回 8（64位系统），但字符串实际长度可能是 100
这会导致循环无法遍历整个字符串，或者访问越界
正确做法：

int size = strlen(s);  // ✅ 正确：获取字符串实际长度

##忘记的代码
**计算字符串长度**
int size = strlen(s);  // ✅ 正确：获取字符串实际长度

## 相关知识点

- 字符串处理：`isalnum()`、`tolower()` 等字符函数的使用
- 双指针技巧：从两端向中间移动的经典模式
- 空间优化：避免构建新字符串，实现 O(1) 空间复杂度

## 类似题目

- 680. 验证回文串 II（允许删除一个字符）
- 9. 回文数
- 234. 回文链表
- 5. 最长回文子串

