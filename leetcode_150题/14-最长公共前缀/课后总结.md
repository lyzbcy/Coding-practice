我发布的题解
（待填写题解链接）

思路关键点
- 使用纵向扫描方法，以第一个字符串为基准，逐个字符位置比较所有字符串
- 外层循环：遍历第一个字符串的每个字符位置 `i`
- 内层循环：遍历所有其他字符串 `strs[1..strsSize-1]`，检查它们的第 `i` 个字符
- 边界检查：必须检查 `i < strlen(strs[j])` 防止越界
- 提前返回：一旦发现不匹配或某个字符串长度不足，立即返回前 `i` 个字符
- 内存管理：C 语言需要使用 `malloc` 动态分配返回字符串的内存，不能返回局部数组

## 复盘感想
（待填写学习心得和反思）


