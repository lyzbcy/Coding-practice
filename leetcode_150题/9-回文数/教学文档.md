# LeetCode 9. 回文数 - 详细教学文档

## 📚 目录

1. [问题分析](#问题分析)
2. [思路解析](#思路解析)
3. [算法详解](#算法详解)
4. [代码实现](#代码实现)
5. [复杂度分析](#复杂度分析)
6. [常见错误](#常见错误)
7. [扩展思考](#扩展思考)

---

## 问题分析

### 题目要求

- 给定一个整数 `x`
- 判断 `x` 是否为回文数
- 回文数：正序和倒序读都一样（如 `121`、`1221`）

### 关键约束

1. **不能转换为字符串**（进阶要求）
2. **负数不是回文数**（如 `-121` 反转后是 `121-`）
3. **整数范围**：`-2³¹ <= x <= 2³¹ - 1`
4. **需要处理边界情况**：0、个位数、以 0 结尾的数（如 `10`）

---

## 思路解析

### 错误直觉 ❌

1. **转换为字符串后比较**：
   - 虽然简单，但不符合进阶要求
   - 需要额外空间存储字符串

2. **使用数组存储每一位数字**：
   - 需要额外空间，且逻辑复杂
   - 容易出错（如数组索引、边界处理）

3. **完全反转数字后比较**：
   - 可能溢出（如 `2147483647` 反转后超出 int 范围）
   - 需要处理溢出情况

### 正确思路 ✅

**方法一：反转一半数字（推荐）**

核心思想：只反转数字的后一半，然后与前一半比较。

- 对于偶数位数：如 `1221`，反转后一半 `21` 得到 `12`，与前一半 `12` 比较
- 对于奇数位数：如 `121`，反转后一半得到 `1`，与前一半 `12` 比较（忽略中间位）

**方法二：完全反转（需要处理溢出）**

- 完全反转数字，然后与原数比较
- 需要处理溢出情况（使用 `long long` 或检查溢出）

---

## 算法详解

### 方法一：反转一半数字（推荐）

#### 算法步骤

1. **处理边界情况**
   - 负数直接返回 `false`
   - 0 是回文数，返回 `true`
   - 以 0 结尾且不为 0 的数（如 `10`）不是回文数

2. **反转后一半数字**
   - 使用取模和除法操作：`digit = x % 10`，`x = x / 10`
   - 反转数：`reversed = reversed * 10 + digit`
   - 停止条件：当 `reversed >= x` 时，说明已经反转了一半

3. **比较前一半和后一半**
   - 偶数位数：`x == reversed`
   - 奇数位数：`x == reversed / 10`（去掉中间位）

#### 伪代码

```
function isPalindrome(x):
    if x < 0: return false
    if x == 0: return true
    if x % 10 == 0: return false  // 以0结尾且不为0
    
    reversed = 0
    while x > reversed:
        reversed = reversed * 10 + x % 10
        x = x / 10
    
    // 偶数位数：x == reversed
    // 奇数位数：x == reversed / 10
    return x == reversed || x == reversed / 10
```

#### 示例演示

**示例 1：`x = 1221`（偶数位数）**

```
初始：x = 1221, reversed = 0
第1次：x = 122, reversed = 1  (1221 % 10 = 1)
第2次：x = 12, reversed = 12  (122 % 10 = 2, 1*10+2=12)
停止：x = 12, reversed = 12, x == reversed → true ✓
```

**示例 2：`x = 121`（奇数位数）**

```
初始：x = 121, reversed = 0
第1次：x = 12, reversed = 1  (121 % 10 = 1)
第2次：x = 1, reversed = 12  (12 % 10 = 2, 1*10+2=12)
停止：x = 1, reversed = 12, x == reversed/10 (1 == 12/10) → true ✓
```

**示例 3：`x = 10`（以0结尾）**

```
初始：x = 10, reversed = 0
第1次：x = 1, reversed = 0  (10 % 10 = 0, 0*10+0=0)
停止：x = 1, reversed = 0, x != reversed && x != reversed/10 → false ✓
```

### 方法二：完全反转（需要处理溢出）

#### 算法步骤

1. **处理边界情况**（同方法一）
2. **完全反转数字**
   - 使用 `long long` 存储反转结果，避免溢出
   - 或者检查溢出：如果 `reversed > INT_MAX / 10`，则溢出
3. **比较原数和反转数**

---

## 代码实现

### C 语言核心代码（方法一：反转一半）

```c
bool isPalindrome(int x) {
    // 负数不是回文数
    if (x < 0) {
        return false;
    }
    
    // 0 是回文数
    if (x == 0) {
        return true;
    }
    
    // 以 0 结尾且不为 0 的数不是回文数
    if (x % 10 == 0) {
        return false;
    }
    
    int reversed = 0;
    // 反转后一半数字
    while (x > reversed) {
        reversed = reversed * 10 + x % 10;
        x /= 10;
    }
    
    // 偶数位数：x == reversed
    // 奇数位数：x == reversed / 10（去掉中间位）
    return x == reversed || x == reversed / 10;
}
```

### C++ 实现（方法一）

```cpp
class Solution {
public:
    bool isPalindrome(int x) {
        if (x < 0) return false;
        if (x == 0) return true;
        if (x % 10 == 0) return false;
        
        int reversed = 0;
        while (x > reversed) {
            reversed = reversed * 10 + x % 10;
            x /= 10;
        }
        
        return x == reversed || x == reversed / 10;
    }
};
```

### C 语言实现（方法二：完全反转，使用 long long）

```c
bool isPalindrome(int x) {
    if (x < 0) return false;
    if (x == 0) return true;
    
    long long original = x;
    long long reversed = 0;
    long long temp = x;
    
    // 完全反转数字
    while (temp > 0) {
        reversed = reversed * 10 + temp % 10;
        temp /= 10;
    }
    
    return original == reversed;
}
```

---

## 复杂度分析

### 时间复杂度

- **O(log₁₀ n)**：其中 n 是数字 x 的位数
  - 需要遍历数字的一半位数（方法一）或全部位数（方法二）
  - 对于 32 位整数，最多 10 位数字，因此时间复杂度可以视为 O(1)

### 空间复杂度

- **O(1)**：只使用了常数额外空间（几个变量）

---

## 常见错误

### 1. 忘记处理负数

```c
// ❌ 错误：没有处理负数
bool isPalindrome(int x) {
    int reversed = 0;
    // ...
}

// ✅ 正确：先判断负数
bool isPalindrome(int x) {
    if (x < 0) return false;
    // ...
}
```

### 2. 忘记处理以 0 结尾的数

```c
// ❌ 错误：10 会被误判为回文数
bool isPalindrome(int x) {
    if (x < 0) return false;
    int reversed = 0;
    // ... 反转逻辑
    // 10 反转后是 1，但 1 != 10，可能返回 false
    // 但如果逻辑有误，可能返回 true
}

// ✅ 正确：先判断以 0 结尾
bool isPalindrome(int x) {
    if (x < 0) return false;
    if (x != 0 && x % 10 == 0) return false;
    // ...
}
```

### 3. 完全反转时溢出

```c
// ❌ 错误：可能溢出
bool isPalindrome(int x) {
    int reversed = 0;
    while (x > 0) {
        reversed = reversed * 10 + x % 10;  // 可能溢出！
        x /= 10;
    }
    // ...
}

// ✅ 正确：使用 long long 或只反转一半
bool isPalindrome(int x) {
    long long reversed = 0;  // 使用 long long
    // ...
}
```

### 4. 反转一半时停止条件错误

```c
// ❌ 错误：停止条件不对
while (x != 0) {  // 会反转全部，可能溢出
    reversed = reversed * 10 + x % 10;
    x /= 10;
}

// ✅ 正确：反转一半就停止
while (x > reversed) {
    reversed = reversed * 10 + x % 10;
    x /= 10;
}
```

### 5. 奇数位数判断错误

```c
// ❌ 错误：只判断了 x == reversed
return x == reversed;  // 对于 121，x=1, reversed=12，会返回 false

// ✅ 正确：同时判断偶数和奇数情况
return x == reversed || x == reversed / 10;
```

---

## 扩展思考

### 1. 如果允许转换为字符串？

```c
bool isPalindrome(int x) {
    if (x < 0) return false;
    char str[12];
    sprintf(str, "%d", x);
    int len = strlen(str);
    for (int i = 0; i < len / 2; i++) {
        if (str[i] != str[len - 1 - i]) {
            return false;
        }
    }
    return true;
}
```

**缺点**：需要额外空间，不符合进阶要求。

### 2. 如何判断回文链表？

参考 LeetCode 234. 回文链表，可以使用：
- 方法一：将链表转为数组后判断
- 方法二：找到中点，反转后半部分，然后比较

### 3. 如何判断回文字符串？

参考 LeetCode 125. 验证回文串，使用双指针从两端向中间比较。

### 4. 如何找出所有回文数？

可以生成回文数，而不是判断。例如：
- 生成所有 n 位回文数
- 在给定范围内查找回文数

---

**总结**：回文数判断的核心是**反转数字**，但要注意：
1. 负数不是回文数
2. 以 0 结尾且不为 0 的数不是回文数
3. 只反转一半可以避免溢出问题
4. 需要区分偶数和奇数位数的情况

