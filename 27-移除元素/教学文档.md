# LeetCode 27. 移除元素 - 详细教学文档

## 📚 目录

1. [问题分析](#问题分析)
2. [思路解析](#思路解析)
3. [算法详解](#算法详解)
4. [代码实现](#代码实现)
5. [复杂度分析](#复杂度分析)
6. [常见错误](#常见错误)
7. [扩展思考](#扩展思考)

---

## 问题分析

### 题目要求

- 给定数组 `nums` 与目标值 `val`
- 原地移除所有等于 `val` 的元素
- 返回移除后数组中不等于 `val` 的元素个数 `k`
- `nums` 的前 `k` 个位置必须是保留下来的元素，顺序任意

### 关键约束

1. **必须原地完成**，额外空间复杂度 O(1)
2. **元素顺序可以改变**，因此不需要稳定性
3. `nums.length` 最多 100，非常适合线性扫描

---

## 思路解析

### 错误直觉 ❌

- **使用额外数组收集答案**：虽然简单，但不符合“原地修改”的要求
- **每次遇到 `val` 就做整体移动**：会造成 O(n²) 的时间复杂度

### 正确思路 ✅

本题是经典的 **双指针（快慢指针）** 模板：

- `fast`：扫描数组，负责找到所有不等于 `val` 的元素
- `slow`：记录下一个安全写入的位置

伪代码：

```
slow = 0
for fast in range(len(nums)):
    if nums[fast] != val:
        nums[slow] = nums[fast]
        slow += 1
return slow
```

---

## 算法详解

1. **初始化指针**
   - `slow = 0`：表示下一个有效元素写入的位置
   - `fast` 从 0 扫描到 `n - 1`

2. **遍历数组**
   - 当 `nums[fast] != val`：
     - 将 `nums[fast]` 复制到 `nums[slow]`
     - `slow` 前进一格
   - 当 `nums[fast] == val`：
     - 跳过即可，由 `fast` 继续前进

3. **结束条件**
   - `fast` 到达数组末尾时，`slow` 的值即为 `k`
   - `nums` 的前 `slow` 个元素已经全部不等于 `val`

4. **为什么可以覆盖？**
   - 当 `fast >= slow` 时，当前位置原本就是待保留区或待覆盖区
   - 即使 `fast == slow`，也是在原地写回，不会丢失数据

---

## 代码实现

### C 语言核心代码

```c
int removeElement(int* nums, int numsSize, int val) {
    int slow = 0;
    for (int fast = 0; fast < numsSize; fast++) {
        if (nums[fast] != val) {
            nums[slow++] = nums[fast];
        }
    }
    return slow;
}
```

### C++（面向对象）实现

```cpp
class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        int slow = 0;
        for (int fast = 0; fast < nums.size(); fast++) {
            if (nums[fast] != val) {
                nums[slow++] = nums[fast];
            }
        }
        return slow;
    }
};
```

---

## 复杂度分析

- **时间复杂度：** O(n)  
  每个元素最多访问一次并且最多写一次。

- **空间复杂度：** O(1)  
  仅使用两个指针变量，属于常数额外空间。

---

## 常见错误

1. **多余的交换操作**  
   使用交换而不是覆盖会增加操作次数，且没有必要。

2. **忘记返回 `slow`**  
   返回 `numsSize - 移除数量` 等写法容易出错，直接返回 `slow` 最清晰。

3. **提前终止循环**  
   不应该在遇到第一个 `val` 后停止，还要继续扫描后续元素。

4. **误以为需要保持原顺序**  
   题目明确允许顺序发生改变，可以直接覆盖。

---

## 扩展思考

1. **与 26 题《删除有序数组中的重复项》的联系**  
   - 同样是快慢指针
   - 差别在于本题的数据无需有序

2. **与 283 题《移动零》类似**  
   - 也是将不满足条件的元素覆盖到前方
   - 不同的是 283 题还需要在尾部填 0

3. **如何处理多条件过滤？**  
   - 将 `nums[fast] != val` 换成任意条件即可
   - 例如：保留偶数、保留大于某个阈值的元素

---

**总结：** 本题是双指针原地覆盖的入门练习，重点在于理解“慢指针标记写入位置、快指针负责寻找合格元素”的协作方式。掌握本题后，类似的原地过滤/压缩问题都可以触类旁通。👍


